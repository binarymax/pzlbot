#!/usr/bin/env node
var debug = require('debug')('my-application');
var app = require('../app');

app.set('port', 8505);

var server = app.listen(app.get('port'), function() {
  debug('PuzzleBot server listening on port ' + server.address().port);
});
